<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog</title>
    <link rel="stylesheet" href="styles/styles.css">
</head>
<body>
    <form method="post" action="/submit">
    <h1>Create a New Post</h1>
    <label>
        Authors Name <input type="text" name="author", value="<%= posts.author%>">
    </label>
    <label>Post Title
            <input type="text" name="title", value="<%= posts.title %>">
    </label>
    <label>Content
           <textarea name="content" rows=2 , ><%= posts.content %></textarea>
    </label>
    <% if(errors) { %>
        <ul class="errors">
            <% errors.forEach((error) => { %>
                <li><%= error %></li>
                <% }); %>
        </ul>
        <% }; %>
    <button type="submit">Submit</button>
    
    </form>


</body>
</html>